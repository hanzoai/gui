{
  "name": "next15-plus-cli-optimize",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "echo skipped - needs full Next.js setup",
    "test:web": "vitest run",
    "test:unit": "vitest run",
    "test:clean": "git checkout packages/app/ && git clean -fd packages/app/"
  },
  "dependencies": {
    "@hanzogui/cli": "workspace:*",
    "@hanzo/gui": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.0.4"
  }
}
