{
  "name": "next15-plus-cli-optimize",
  "version": "8.3.1",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "vitest run; s=$?; bun run test:clean; exit $s",
    "test:web": "vitest run; s=$?; bun run test:clean; exit $s",
    "test:unit": "vitest run",
    "test:clean": "git checkout packages/app/ && git clean -fd packages/app/"
  },
  "dependencies": {
    "@hanzo/gui": "workspace:*",
    "@hanzogui/cli": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
