{
  "name": "next15-plus-cli-optimize",
  "version": "2.0.0-rc.29",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:web": "vitest run",
    "test:unit": "vitest run",
    "test:clean": "git checkout packages/app/ && git clean -fd packages/app/"
  },
  "dependencies": {
    "@hanzo/gui-cli": "workspace:*",
    "@hanzo/gui": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.0.4"
  }
}
